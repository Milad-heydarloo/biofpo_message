name: biofpo_message
description: "پروژه نمایش پیغام با موضوع، توضیحات و رنگ."
publish_to: "https://pub.dev" # مقدار 'none' را حذف کردیم

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
